(defwidget vpn-aws [ state ]
  (box
    (button
      :onclick `$HOME/.config/eww/scripts/vpn-toggle.sh AWS`
      :class { state == '0' ? "vpn-off" : "vpn-on" }
      (label
        :text { state == '0' ? "" : "" }
      )
    )
  )
)
